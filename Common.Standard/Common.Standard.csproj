<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <Authors>Jeffrey Bramlett</Authors>
    <Company>BlueJaySvc</Company>
    <Description>.Net Standard library of extensions and generics to be used in any application.
</Description>
    <Copyright>Jeffrey Bramlett @December 2018</Copyright>
    <PackageLicenseUrl>not applicable</PackageLicenseUrl>
    <PackageProjectUrl>https://github.com/JeffBramlett/Common.Standard</PackageProjectUrl>
    <RepositoryUrl>https://github.com/JeffBramlett/Common.Standard</RepositoryUrl>
    <RepositoryType>GitHub</RepositoryType>
    <PackageTags>common,generics,extensions</PackageTags>
    <PackageReleaseNotes>Common extensions, classes and abstractions
* added List&lt;/T&gt; extension "TryFindItem(predicate, out T found): Try to find using a predicate from a list.  (does null check and does not throw exception)
* added List&lt;/T&gt; extension "TryFindValue(predicate, out T found): Try to find using a predicate from a list.  (does null check and does not throw exception)
* added List&lt;T&gt; extension "TryFindAll for using one or more predicates to find items in a list (does null check and does not throw exception)
* - Changed ServiceLoader to recursively load from the starting path</PackageReleaseNotes>
    <Version>1.0.3</Version>
    <AssemblyVersion>1.0.1.3</AssemblyVersion>
    <FileVersion>1.0.1.3</FileVersion>
  </PropertyGroup>

</Project>
